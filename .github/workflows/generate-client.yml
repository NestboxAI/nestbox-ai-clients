name: Generate API Client
on:
  push:
    branches:
        - '*'

jobs:
  generate-client:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Download OpenAPI Spec
        run: curl -o openapi.json https://demo-api.nestbox.ai/v1/api-json


      - name: Show existing files
        run: ls -la

      - name: Install OpenAPI Generator CLI
        run: npm install @openapitools/openapi-generator-cli -g

      - name: Validate OpenAPI Spec
        run: openapi-generator-cli validate -i openapi.json

      - name: Generate API Client
        run: |
          openapi-generator-cli generate \
            -i openapi.json \
            -g typescript-axios \
            -o generated-client \
            --skip-validate-spec \
            --additional-properties=supportsES6=true,npmName=@nestbox/client,npmVersion=1.0.0

      - name: Commit and Push Generated Client
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git remote set-url origin https://x-access-token:${{ secrets.PAT_TOKEN }}@github.com/NestboxAI/nestbox-ai-dashboard-api-client.git
          git add generated-client
          git commit -m "Automatically generated API client from OpenAPI spec" || echo "No changes to commit"
          git push origin HEAD:${{ github.ref }}
